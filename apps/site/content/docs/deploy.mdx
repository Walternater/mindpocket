---
title: 部署说明
description: 面向自托管场景的最小部署建议，包括站点、数据库与运行时依赖。
---

# 部署说明

## 推荐组合

对个人使用场景，推荐以下组合：

- 前端：Vercel
- 数据库：Neon PostgreSQL
- 认证：Better Auth

## 站点拆分

这个仓库当前包含两个主要 Web 入口：

- `apps/site` 提供官网与 `/docs`
- `apps/web` 提供登录后的产品能力

可以分别部署，也可以按你的基础设施策略合并处理。

## 部署前检查

1. 确认 `pnpm install` 成功
2. 配置数据库连接与认证环境变量
3. 为 `apps/web` 完成数据库初始化
4. 验证 `pnpm build` 或按应用分别构建

## 数据库相关

开发阶段可使用：

```bash
pnpm --filter web db:push
```

生产环境建议走迁移流程，而不是直接推送 schema。
